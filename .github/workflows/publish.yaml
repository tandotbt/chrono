name: publish

on:
  push:
    branches:
      - "main"

jobs:
  jsr:
    uses: planetarium/.github/.github/workflows/publish_jsr.yaml@e956907c32272dc5c0a2cd670cf015539fab86bd
    with:
      working_directory: "packages/chrono-sdk"
      pnpm_version: 9.1.2

