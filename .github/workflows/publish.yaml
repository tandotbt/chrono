name: publish

on:
  push:
    branches:
      - "main"

jobs:
  jsr:
    uses: planetarium/.github/.github/workflows/publish_jsr.yaml@acc4239d3b326fc9e7307279fc0b9d4fc23bc55f
    with:
      working_directory: "packages/chrono-sdk"
      pnpm_version: "9.1.2"
